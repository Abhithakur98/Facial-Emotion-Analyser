# ImbesideyouAssignment

A facial emotion analyser/detector using React JS & the face-api.js(which uses TensorFlow) . It consists of a photo capture mode with a gallery, and a real-time face detection mode.
